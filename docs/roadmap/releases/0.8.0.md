# Release 0.8.0

- Status: Released on 2026-04-09

## Intent

Move Code Orb from an experimental CLI demo toward a CLI that is genuinely usable for real local-repository work.

`0.8.0` is the milestone where the runtime and shell should become reliable enough that contributors can use Code Orb for a meaningful subset of day-to-day coding tasks without treating every run as a fragile demo.

This is still not the same thing as a fully stable `1.0.0` CLI contract. The goal is usable and dependable, not frozen.

## In Scope

- harden the end-to-end CLI task loop for common local-repository work:
  - inspect
  - plan
  - edit
  - verify
  - report
- improve success and recovery behavior for common coding tasks after the `0.7.0` runtime skeleton, tool orchestration boundary, and provider adapter baseline are stable
- reduce the number of task classes that still depend on narrow special cases or fragile prompting
- improve error reporting and recovery guidance when provider, tool, verification, or approval paths fail
- make approval behavior and mutating-action reporting clearer during real CLI use
- support repository-level agent instructions well enough for real project use:
  - discover and read `AGENTS.md` or the authoritative project instruction surface
  - inject that guidance into execution predictably
  - make instruction use visible enough that users can tell the runtime is operating with project context
- expand benchmark and fixture coverage so CLI usability is measured against more realistic tasks than the current demo path
- tighten session and turn reporting so users can understand what happened without reading raw artifacts directly

## Out Of Scope

- a major TUI redesign or terminal UX overhaul
- desktop shells or desktop UI implementation
- plugin, MCP, or external extension ecosystems as product priorities
- broad multi-provider routing as a product feature
- background daemon mode
- core runtime layering changes that belong to `0.7.0`
- a stable `1.0.0` public CLI contract

## Exit Criteria

- Code Orb can complete a meaningful set of local-repository coding tasks with predictable inspect, edit, verify, and report behavior
- common failure paths produce actionable reporting instead of silent stalls or opaque errors
- approval and mutation reporting are understandable during normal CLI use
- repository instruction handling is reliable enough that Code Orb does not behave like a blank-slate agent on a guided project
- benchmark and test coverage demonstrate reliability beyond the current narrow demo path
- `orb run`, `orb chat`, session artifacts, and the current interactive benchmark remain functional
- the project can honestly describe the CLI as usable for real local work, even if the public contract is not yet `1.0.0` stable

## Working Focus

`0.8.0` should prioritize trustworthiness and operator confidence over visual polish.

It should assume the `0.7.0` runtime skeleton is already explicit enough that `0.8.0` can focus on reliability instead of reopening provider transport or session-versus-turn architecture.

The milestone question is:

- can a contributor reach for Code Orb for real local coding tasks without mentally classifying it as a demo every time?

## Kickoff Notes

Development for `0.8.0` started on 2026-04-09 under the workflow defined in `docs/engineering/release-workflow.md`.

At kickoff:

- the release doc already existed, but the execution checklist did not
- the current codebase does not yet contain an explicit repository-instruction discovery and injection path for `AGENTS.md`
- the first implementation priority is therefore repository instruction handling plus common task reliability on top of the `0.7.0` runtime skeleton

## Suggested Task Packages

### 1. Common Task Reliability

- harden the paths that should work repeatedly in ordinary repository tasks
- reduce hidden task-specific fragility

### 2. Error And Recovery Quality

- improve failure reporting
- improve repair and fallback behavior where the runtime can act safely, on top of the adapter-owned fallback rules already established in `0.7.0`

### 3. Approval And Reporting Clarity

- make mutating actions and their outcomes easier to understand in real-time and in saved artifacts

### 4. Project Instructions

- make repository guidance such as `AGENTS.md` part of normal CLI execution rather than a manual workaround

### 5. Benchmark And Fixture Expansion

- validate usability against a broader set of realistic local tasks

## Execution Plan

Use the ordered release checklist to execute and review this milestone:

- [0.8.0-checklist.md](./0.8.0-checklist.md)

Execution order:

1. `P0: Release Kickoff Boundary And Acceptance Baseline`
2. `P0: Repository Instruction Intake And Visibility`
3. `P1: Common Task Reliability And Recovery`
4. `P1: Approval And Mutation Reporting Clarity`
5. `P1: Session And Turn Reporting Usability`
6. `P2: Benchmarks, Fixture Expansion, Acceptance Triage, And Docs`

## Execution Notes

- initial landing date: 2026-04-09
- release closeout date: 2026-04-09
- 2026-04-09: landed repository-root `AGENTS.md` discovery, runtime-owned planning-context injection, and visible instruction reporting in events, turn reports, session reports, and persisted session artifacts
- 2026-04-09: landed approval lifecycle events, mutating-action reporting, richer session artifacts, and compatibility/fallback notes in turn/session reports
- 2026-04-09: tightened model-tool-loop failure handling so denied or failed tool requests terminate the turn with actionable reporting instead of falling through to a misleading completion
- 2026-04-09: closeout validation found that `.orb/sessions` artifacts could be selected as replace targets after earlier local runs; `0.8.0` fixed this by excluding `.orb/` from repository search/listing helpers and by adding CLI regression coverage

## Acceptance Baseline

`0.8.0` kept this baseline green through implementation and closeout:

- `orb run`
- `orb chat`
- session artifacts and `sessions show`
- approval-required mutating flows
- benchmark coverage:
  - `approval-denied`
  - `existing-changes-awareness`
  - `failing-test-fix`
  - `interactive-multi-turn`
  - `project-instructions`
  - `session-follow-up`
  - `verification-still-fails`

ADR decision for `0.8.0`:

- no new ADR was required
- architecture and protocol docs were updated in place because the release clarified reporting, instruction intake, and event usage without changing the harder-to-reverse runtime boundary decisions already recorded by earlier ADRs

## Validation Strategy

### Unit Test

- error classification and reporting paths
- approval and mutation-reporting behavior
- project-instruction discovery and injection behavior
- common task-shape helpers that remain in the runtime

### Integration Test

- realistic local task flows across inspect, edit, verify, and report
- recovery behavior after provider, tool, or verification failures
- repository-guided task flows that depend on project instructions being honored

### End-To-End Or Benchmark

- existing benchmark coverage still passes
- additional milestone benchmarks represent real CLI usage rather than only demo-style paths

## Acceptance Findings

- summary: replace-and-verify repository search could select `.orb/sessions` artifacts after earlier local runs instead of the intended working file
- found during: manual `orb run` TTY validation on 2026-04-09
- affected flow/test/benchmark: primary `orb run` replace-and-verify path after session artifacts already existed in the working directory
- scope relation: Current Release Closeout Fix
- decision: fixed in `0.8.0` by excluding `.orb/` from repository search/listing helpers and adding CLI e2e coverage
- target version: `0.8.0`
- release blocking: yes

## Current Validation Status

Validation on 2026-04-09 covered:

- `pnpm build`
- `pnpm test`
- `pnpm run benchmark:approval-denied`
- `pnpm run benchmark:existing-changes-awareness`
- `pnpm run benchmark:failing-test-fix`
- `pnpm run benchmark:interactive-multi-turn`
- `pnpm run benchmark:project-instructions`
- `pnpm run benchmark:session-follow-up`
- `pnpm run benchmark:verification-still-fails`

Manual execution coverage on 2026-04-09 also covered:

- one-shot `orb run` in a temp fixture repository through real TTY approvals for `apply_patch` and `run_command`
- interactive `orb chat` in a temp fixture repository through real TTY approvals, `/status`, and `/exit`

This validation round confirmed:

- repository guidance is discovered and surfaced during normal CLI execution and in persisted artifacts
- approval-required mutating flows are visible in both runtime events and final reports
- compatibility and fallback notes survive into turn/session reporting instead of being visible only in assistant-event payloads
- model-tool-loop failures now stop with actionable tool-failure reporting
- saved `.orb/` session artifacts no longer pollute repository search targets for normal edit tasks
- `orb run`, `orb chat`, session artifacts, benchmark coverage, and the current e2e suite remained functional after the `0.8.0` reliability work

## Release Validation Status

As of 2026-04-09, `0.8.0` is considered released because:

- Code Orb can complete the current meaningful set of local inspect, edit, verify, and report tasks with explicit approval handling and clearer mutation reporting
- common failure paths now produce more actionable stop reasons, notes, and mutating-action history instead of only opaque terminal failures
- repository instruction handling is runtime-owned and visible enough that guided projects no longer look like blank-slate execution
- test, benchmark, and manual execution coverage now extend beyond the narrow original demo path
- the remaining limitations are honest `0.9.0+` issues rather than hidden blockers for calling the CLI usable in its current bounded scope

## Remaining Risks And Deferred Work

- task interpretation is still intentionally narrow and heuristic in places; broader task-shape coverage and richer terminal ergonomics remain `0.9.0` work
- repository guidance currently covers repository-root `AGENTS.md`; broader instruction, identity, and configuration surfaces remain `0.10.0` work
- the CLI is usable in its current bounded local-repository scope, but it is not yet a broadly capable general coding agent or a stable `1.0.0` contract
