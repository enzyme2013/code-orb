# Mini Utils

`mini-utils` is a small TypeScript utility library used as a benchmark repository for Code Orb.

## Available Utilities

- `chunk`
- `createSlug`
- `toTitleCase`

## Verification

Run the benchmark verification from this directory:

```bash
node verify.mjs
```

The benchmark intentionally starts with one failing behavior.
