Start one interactive `orb chat` session.

Within that same session:

1. update `README.md` by replacing `__CODE_ORB_PLACEHOLDER__` with `Hello, Code Orb!` and run `node verify.mjs`
2. issue a follow-up request that continues the prior work in the same session
3. exit the interactive session cleanly

Success means one saved session artifact contains both turns and the final output stays understandable.
