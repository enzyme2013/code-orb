# Interactive Multi-Turn Benchmark

This benchmark validates the `0.5.0` interactive CLI milestone.

It starts one foreground interactive session, completes one edit-and-verify turn, then completes a second follow-up turn in the same session before exiting.
